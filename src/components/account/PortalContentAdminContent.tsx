"use client";

import { ArrowDown, ArrowUp, ChevronLeft, ChevronRight, ImagePlus, LoaderCircle, Megaphone, Pencil, Plus, Save, Search, Trash2, Upload, WandSparkles } from "lucide-react";
import { ChangeEvent, useCallback, useEffect, useMemo, useState } from "react";
import { useModals } from "@/components/providers";
import { ConsoleEmptyState, ConsolePage } from "@/components/console/ConsolePage";
import { Button } from "@/components/ui/button";

type Category = "llm" | "image" | "audio" | "video" | "embed" | "other";
type Slide = { id: string; imageUrl: string; alt: string; href: string; enabled: boolean };
type Notice = { id: string; title: string; body: string; href: string; enabled: boolean; createdAt: string };
type Vendor = { id: string; name: string; key: string; logoUrl: string; category: Category; enabled: boolean; models: Array<{ name: string; endpointTypes: string[]; description?: string }> };
type PortalContent = { carousel: Slide[]; notifications: Notice[]; modelVendors: Vendor[] };
type CatalogModel = { model_name: string; vendor_key?: string; vendor_name?: string; vendor_logo?: string; portal_category?: Category; supported_endpoint_types?: string[] };

const categories: Array<{ value: Category; label: string }> = [
  { value: "llm", label: "语言推理" }, { value: "image", label: "图像处理" }, { value: "audio", label: "音频处理" }, { value: "video", label: "视频处理" }, { value: "embed", label: "RAG 知识库" }, { value: "other", label: "信息检索" },
];
const emptyContent: PortalContent = { carousel: [], notifications: [], modelVendors: [] };
const uid = (prefix: string) => `${prefix}-${Math.random().toString(36).slice(2, 9)}`;
const categoryLabel = (value: Category) => categories.find((item) => item.value === value)?.label ?? value;

function modelsToText(models: Vendor["models"]) { return models.map((model) => `${model.name}|${model.endpointTypes.join(",")}|${model.description ?? ""}`).join("\n"); }
function textToModels(value: string): Vendor["models"] { return value.split("\n").map((line) => line.trim()).filter(Boolean).map((line) => { const [name, types = "chat", description = ""] = line.split("|"); return { name: name.trim(), endpointTypes: types.split(",").map((type) => type.trim()).filter(Boolean), description: description.trim() || undefined }; }).filter((model) => model.name); }
function categoryFromModel(model: CatalogModel): Category { const text = `${model.model_name} ${(model.supported_endpoint_types ?? []).join(" ")}`.toLowerCase(); if (model.portal_category) return model.portal_category; if (text.includes("image") || text.includes("dall") || text.includes("flux")) return "image"; if (text.includes("video") || text.includes("sora") || text.includes("kling")) return "video"; if (text.includes("audio") || text.includes("speech") || text.includes("whisper")) return "audio"; if (text.includes("embed") || text.includes("rerank") || text.includes("rag")) return "embed"; if (text.includes("search") || text.includes("retrieval")) return "other"; return "llm"; }
function groupCatalog(models: CatalogModel[]): Vendor[] { const grouped = new Map<string, Vendor>(); for (const model of models) { const key = (model.vendor_key || model.vendor_name || "other").trim().toLowerCase().replace(/[^a-z0-9_-]/g, "-") || "other"; const current = grouped.get(key) ?? { id: `catalog-${key}`, name: model.vendor_name || key, key, logoUrl: model.vendor_logo || "/vendors/other.svg", category: categoryFromModel(model), enabled: true, models: [] }; current.models.push({ name: model.model_name, endpointTypes: model.supported_endpoint_types ?? ["chat"] }); grouped.set(key, current); } return [...grouped.values()].sort((a, b) => a.name.localeCompare(b.name)); }

async function readImage(event: ChangeEvent<HTMLInputElement>, onRead: (url: string) => void) { const file = event.target.files?.[0]; if (!file) return; if (!file.type.startsWith("image/") || file.size > 700_000) throw new Error("请上传 700KB 以内的图片文件，保证完整保存与快速加载。"); const reader = new FileReader(); reader.onload = () => typeof reader.result === "string" && onRead(reader.result); reader.readAsDataURL(file); }

function NotificationManager({ notifications, onChange, onSave, saving }: { notifications: Notice[]; onChange: (next: Notice[]) => void; onSave: () => void; saving: boolean }) {
  const [query, setQuery] = useState(""); const [status, setStatus] = useState<"all" | "enabled" | "disabled">("all"); const [page, setPage] = useState(0); const pageSize = 8;
  const filtered = useMemo(() => notifications.map((item, index) => ({ item, index })).filter(({ item }) => (status === "all" || (status === "enabled" ? item.enabled : !item.enabled)) && `${item.title} ${item.body}`.toLowerCase().includes(query.trim().toLowerCase())), [notifications, query, status]);
  const pages = Math.max(1, Math.ceil(filtered.length / pageSize)); const currentPage = Math.min(page, pages - 1); const visible = filtered.slice(currentPage * pageSize, currentPage * pageSize + pageSize);
  const update = (index: number, patch: Partial<Notice>) => onChange(notifications.map((item, itemIndex) => itemIndex === index ? { ...item, ...patch } : item));
  const move = (index: number, direction: -1 | 1) => { const target = index + direction; if (target < 0 || target >= notifications.length) return; const next = [...notifications]; [next[index], next[target]] = [next[target], next[index]]; onChange(next); };
  return <section className="grid gap-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="font-semibold">通知管理 <span className="text-sm font-normal text-muted-foreground">({notifications.length})</span></h2><p className="text-sm text-muted-foreground">右上角通知入口会展示已启用的内容。支持多条通知、筛选、排序与分页管理。</p></div><div className="flex gap-2"><Button variant="outline" size="sm" type="button" onClick={() => { onChange([{ id: uid("notice"), title: "新通知", body: "请填写通知内容", href: "/", enabled: true, createdAt: new Date().toISOString() }, ...notifications]); setPage(0); }}><Megaphone className="h-4 w-4" />新增通知</Button><Button size="sm" type="button" disabled={saving} onClick={onSave}><Save className="h-4 w-4" />保存通知</Button></div></div><div className="flex flex-wrap gap-2 rounded-xl border border-border bg-muted/20 p-3"><label className="flex h-9 min-w-56 flex-1 items-center gap-2 rounded-md border border-border bg-background px-3"><Search className="h-4 w-4 text-muted-foreground" /><input className="w-full border-0 bg-transparent text-sm outline-none" value={query} onChange={(event) => { setQuery(event.target.value); setPage(0); }} placeholder="搜索标题或正文" /></label><select className="h-9 rounded-md border border-border bg-background px-3 text-sm" value={status} onChange={(event) => { setStatus(event.target.value as typeof status); setPage(0); }}><option value="all">全部状态</option><option value="enabled">仅已启用</option><option value="disabled">仅已停用</option></select><span className="self-center text-xs text-muted-foreground">显示 {visible.length} / {filtered.length}</span></div>{visible.length ? <div className="grid gap-3">{visible.map(({ item, index }) => <article className="grid gap-3 rounded-xl border border-border bg-background p-4 md:grid-cols-[1fr_1fr_auto]" key={item.id}><div className="grid gap-2"><input className="h-9 rounded-md border border-border px-3 text-sm" value={item.title} placeholder="通知标题" onChange={(event) => update(index, { title: event.target.value })} /><textarea className="min-h-20 rounded-md border border-border p-3 text-sm" value={item.body} placeholder="通知正文" onChange={(event) => update(index, { body: event.target.value })} /></div><div className="grid content-start gap-2"><input className="h-9 rounded-md border border-border px-3 text-sm" value={item.href} placeholder="点击跳转地址" onChange={(event) => update(index, { href: event.target.value })} /><p className="text-xs text-muted-foreground">创建时间：{new Date(item.createdAt).toLocaleString("zh-CN")}</p></div><div className="flex items-start justify-end gap-2"><label className="flex items-center gap-1 text-xs"><input type="checkbox" checked={item.enabled} onChange={(event) => update(index, { enabled: event.target.checked })} />启用</label><Button variant="ghost" size="icon" type="button" disabled={index === 0} onClick={() => move(index, -1)}><ArrowUp className="h-4 w-4" /></Button><Button variant="ghost" size="icon" type="button" disabled={index === notifications.length - 1} onClick={() => move(index, 1)}><ArrowDown className="h-4 w-4" /></Button><Button variant="ghost" size="icon" type="button" onClick={() => onChange(notifications.filter((_, itemIndex) => itemIndex !== index))}><Trash2 className="h-4 w-4 text-red-500" /></Button></div></article>)}</div> : <ConsoleEmptyState title="没有符合条件的通知" description="可调整筛选条件或新增一条通知。" />}{pages > 1 ? <div className="flex items-center justify-end gap-2"><Button variant="outline" size="sm" type="button" disabled={currentPage === 0} onClick={() => setPage((current) => current - 1)}><ChevronLeft className="h-4 w-4" />上一页</Button><span className="text-xs text-muted-foreground">{currentPage + 1} / {pages}</span><Button variant="outline" size="sm" type="button" disabled={currentPage >= pages - 1} onClick={() => setPage((current) => current + 1)}>下一页<ChevronRight className="h-4 w-4" /></Button></div> : null}</section>;
}

function VendorEditor({ vendors, catalogVendors, onChange, onSave, saving }: { vendors: Vendor[]; catalogVendors: Vendor[]; onChange: (next: Vendor[]) => void; onSave: () => void; saving: boolean }) {
  const addVendor = () => onChange([...vendors, { id: uid("vendor"), name: "新厂商", key: uid("vendor"), logoUrl: "", category: "llm", enabled: true, models: [{ name: "新模型", endpointTypes: ["chat"] }] }]);
  const update = (index: number, patch: Partial<Vendor>) => onChange(vendors.map((item, itemIndex) => itemIndex === index ? { ...item, ...patch } : item));
  const importVendor = (vendor: Vendor) => { if (vendors.some((item) => item.key === vendor.key)) return; onChange([...vendors, { ...vendor, id: uid("vendor") }]); };
  return <section className="grid gap-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="font-semibold">API 模型厂商</h2><p className="text-sm text-muted-foreground">按六大类配置厂商、图标和展示模型。导入现有目录后可直接编辑。</p></div><div className="flex gap-2"><Button variant="outline" size="sm" type="button" onClick={addVendor}><WandSparkles className="h-4 w-4" />新增厂商</Button><Button size="sm" type="button" disabled={saving} onClick={onSave}><Save className="h-4 w-4" />保存模型配置</Button></div></div>{vendors.map((vendor, index) => <article key={vendor.id} className="grid gap-3 rounded-xl border border-border bg-background p-4"><div className="grid gap-2 md:grid-cols-[120px_1fr_1fr_180px]"><label className="grid h-20 place-items-center overflow-hidden rounded-lg border border-dashed border-border cursor-pointer">{vendor.logoUrl ? <img src={vendor.logoUrl} alt="" className="h-16 max-w-24 object-contain" /> : <span className="text-xs text-muted-foreground"><Upload className="mx-auto mb-1 h-4 w-4" />上传图标</span>}<input className="hidden" type="file" accept="image/*" onChange={(event) => { void readImage(event, (url) => update(index, { logoUrl: url })).catch((reason) => window.alert(reason.message)); }} /></label><input className="h-9 rounded-md border border-border px-3 text-sm" value={vendor.name} placeholder="厂商名称" onChange={(event) => update(index, { name: event.target.value })} /><input className="h-9 rounded-md border border-border px-3 text-sm" value={vendor.key} placeholder="厂商标识（英文）" onChange={(event) => update(index, { key: event.target.value })} /><select className="h-9 rounded-md border border-border px-3 text-sm" value={vendor.category} onChange={(event) => update(index, { category: event.target.value as Category })}>{categories.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></div><textarea className="min-h-24 rounded-md border border-border p-3 font-mono text-xs" value={modelsToText(vendor.models)} onChange={(event) => update(index, { models: textToModels(event.target.value) })} /><div className="flex justify-between"><label className="flex items-center gap-1 text-xs"><input type="checkbox" checked={vendor.enabled} onChange={(event) => update(index, { enabled: event.target.checked })} />在目录展示</label><Button type="button" variant="ghost" size="sm" onClick={() => onChange(vendors.filter((_, itemIndex) => itemIndex !== index))}><Trash2 className="mr-1 h-4 w-4 text-red-500" />删除厂商</Button></div></article>)}<section className="grid gap-3 rounded-xl border border-dashed border-primary-200 bg-primary-50/30 p-4"><div><h3 className="font-semibold">已同步 API 模型目录</h3><p className="text-sm text-muted-foreground">当前网关返回 {catalogVendors.reduce((total, vendor) => total + vendor.models.length, 0)} 个模型、{catalogVendors.length} 个厂商。点击“导入并编辑”后才会写入配置草稿。</p></div><div className="grid gap-2 md:grid-cols-2 xl:grid-cols-3">{catalogVendors.map((vendor) => <details key={vendor.key} className="rounded-lg border border-border bg-background p-3"><summary className="cursor-pointer list-none"><div className="flex items-center gap-2"><img src={vendor.logoUrl} alt="" className="h-6 w-6 rounded object-contain" /><strong className="text-sm">{vendor.name}</strong><span className="ml-auto text-xs text-muted-foreground">{vendor.models.length} 个模型</span></div><span className="mt-1 block text-xs text-primary-600">{categoryLabel(vendor.category)}</span></summary><ul className="mt-3 grid gap-1 border-t border-border pt-2 text-xs text-muted-foreground">{vendor.models.map((model) => <li key={model.name}>{model.name}<span className="ml-1 text-[10px]">{model.endpointTypes.join(" · ")}</span></li>)}</ul><Button className="mt-3" variant="outline" size="sm" type="button" disabled={vendors.some((item) => item.key === vendor.key)} onClick={() => importVendor(vendor)}><Pencil className="h-3.5 w-3.5" />{vendors.some((item) => item.key === vendor.key) ? "已在配置中" : "导入并编辑"}</Button></details>)}</div></section></section>;
}

export default function PortalContentAdminContent() {
  const { account, accountLoading } = useModals(); const [content, setContent] = useState<PortalContent>(emptyContent); const [catalogVendors, setCatalogVendors] = useState<Vendor[]>([]); const [loading, setLoading] = useState(true); const [savingSection, setSavingSection] = useState<"carousel" | "notifications" | "models" | null>(null); const [notice, setNotice] = useState(""); const [error, setError] = useState("");
  const load = useCallback(async () => { setLoading(true); setError(""); try { const [settings, catalog] = await Promise.all([fetch("/api/admin/portal-content", { credentials: "same-origin" }), fetch("/api/catalog/plaza", { credentials: "same-origin", cache: "no-store" })]); const body = await settings.json() as PortalContent & { error?: string }; if (!settings.ok) throw new Error(body.error || "加载失败"); setContent({ carousel: body.carousel ?? [], notifications: body.notifications ?? [], modelVendors: body.modelVendors ?? [] }); const payload = await catalog.json().catch(() => null) as { data?: CatalogModel[] } | null; setCatalogVendors(groupCatalog(payload?.data ?? [])); } catch (reason) { setError(reason instanceof Error ? reason.message : "加载失败"); } finally { setLoading(false); } }, []);
  useEffect(() => { if (account?.platform_role !== "admin") return; const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, [account?.platform_role, load]);
  const save = async (section: NonNullable<typeof savingSection>) => { setSavingSection(section); setNotice(""); setError(""); try { const response = await fetch("/api/admin/portal-content", { method: "PUT", headers: { "content-type": "application/json" }, credentials: "same-origin", body: JSON.stringify(content) }); const body = await response.json() as PortalContent & { error?: string }; if (!response.ok) throw new Error(body.error || "保存失败"); setContent({ carousel: body.carousel, notifications: body.notifications, modelVendors: body.modelVendors }); setNotice(section === "carousel" ? "轮播图已发布。" : section === "notifications" ? "通知已发布。" : "模型厂商配置已发布。"); } catch (reason) { setError(reason instanceof Error ? reason.message : "保存失败"); } finally { setSavingSection(null); } };
  if (accountLoading) return <div className="flex items-center gap-2 p-6 text-sm text-muted-foreground"><LoaderCircle className="h-4 w-4 animate-spin" />正在确认账户…</div>;
  if (account?.platform_role !== "admin") return <ConsolePage title="门户内容管理" description="仅平台管理员可以管理首页内容、通知与 API 模型展示。"><ConsoleEmptyState title="没有权限" description="当前账户不是平台 admin。" /></ConsolePage>;
  return <ConsolePage eyebrow="平台" title="门户内容管理" description="轮播、通知、API 模型分别保存与发布到个人门户。">{loading ? <p className="flex items-center gap-2 text-sm text-muted-foreground"><LoaderCircle className="h-4 w-4 animate-spin" />正在加载配置…</p> : <div className="grid gap-8">{notice ? <p className="rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800">{notice}</p> : null}{error ? <p className="rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-700">{error}</p> : null}<section className="grid gap-3"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="font-semibold">首页轮播图</h2><p className="text-sm text-muted-foreground">上传封面或粘贴图片地址，启用后显示在首页中部轮播。</p></div><div className="flex gap-2"><Button variant="outline" size="sm" type="button" onClick={() => setContent((current) => ({ ...current, carousel: [...current.carousel, { id: uid("slide"), imageUrl: "", alt: "新轮播图", href: "/products?cate=api", enabled: true }] }))}><Plus className="h-4 w-4" />新增轮播</Button><Button size="sm" type="button" disabled={savingSection !== null} onClick={() => void save("carousel")}><Save className="h-4 w-4" />保存轮播</Button></div></div>{content.carousel.map((slide, index) => <article key={slide.id} className="grid gap-3 rounded-xl border border-border p-3 md:grid-cols-[150px_1fr_auto]"><label className="grid min-h-24 place-items-center overflow-hidden rounded-lg border border-dashed border-border bg-muted/30 cursor-pointer">{slide.imageUrl ? <img src={slide.imageUrl} alt="" className="h-24 w-full object-contain" /> : <span className="grid place-items-center gap-1 text-xs text-muted-foreground"><ImagePlus className="h-5 w-5" />上传封面</span>}<input className="hidden" type="file" accept="image/*" onChange={(event) => { void readImage(event, (url) => setContent((current) => ({ ...current, carousel: current.carousel.map((item, itemIndex) => itemIndex === index ? { ...item, imageUrl: url } : item) }))).catch((reason) => setError(reason.message)); }} /></label><div className="grid gap-2"><input className="h-9 rounded-md border border-border px-3 text-sm" value={slide.alt} placeholder="轮播标题" onChange={(event) => setContent((current) => ({ ...current, carousel: current.carousel.map((item, itemIndex) => itemIndex === index ? { ...item, alt: event.target.value } : item) }))} /><input className="h-9 rounded-md border border-border px-3 text-sm" value={slide.href} placeholder="跳转地址" onChange={(event) => setContent((current) => ({ ...current, carousel: current.carousel.map((item, itemIndex) => itemIndex === index ? { ...item, href: event.target.value } : item) }))} /><input className="h-9 rounded-md border border-border px-3 text-sm" value={slide.imageUrl.startsWith("data:") ? "已上传本地图片" : slide.imageUrl} placeholder="或粘贴图片 URL" onChange={(event) => !slide.imageUrl.startsWith("data:") && setContent((current) => ({ ...current, carousel: current.carousel.map((item, itemIndex) => itemIndex === index ? { ...item, imageUrl: event.target.value } : item) }))} /></div><div className="flex items-start gap-2"><label className="flex items-center gap-1 text-xs"><input type="checkbox" checked={slide.enabled} onChange={(event) => setContent((current) => ({ ...current, carousel: current.carousel.map((item, itemIndex) => itemIndex === index ? { ...item, enabled: event.target.checked } : item) }))} />启用</label><Button type="button" variant="ghost" size="icon" onClick={() => setContent((current) => ({ ...current, carousel: current.carousel.filter((_, itemIndex) => itemIndex !== index) }))}><Trash2 className="h-4 w-4 text-red-500" /></Button></div></article>)}</section><NotificationManager notifications={content.notifications} onChange={(notifications) => setContent((current) => ({ ...current, notifications }))} onSave={() => void save("notifications")} saving={savingSection !== null} /><VendorEditor vendors={content.modelVendors} catalogVendors={catalogVendors} onChange={(modelVendors) => setContent((current) => ({ ...current, modelVendors }))} onSave={() => void save("models")} saving={savingSection !== null} /></div>}</ConsolePage>;
}
